from datetime import date # Importar Modulo "date" necesario para continuar ("from" o "de" datetime "import" o importa date)

date.today()

print("la fecha de hoy es " + str(date.today())) # para usar el operador + se debe convertir a strings la fecha

